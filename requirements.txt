# Direct dependencies. Exact versions of the full transitive tree are pinned in
# requirements.lock.txt.
#
# torch must come from PyTorch's own index, not PyPI -- the PyPI Windows wheel
# is CPU-only:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# triton is required for torch.compile and is not shipped with PyTorch on
# Windows; install triton-windows there and triton elsewhere.

torch>=2.11
numpy>=2.0

# Serving. These were installed ad hoc during development and never declared,
# so the repository built fine on this machine and failed on a clean checkout
# with ModuleNotFoundError -- found by building the container, which is the
# first environment that had no developer history to fall back on.
grpcio>=1.83             # replica gRPC server, router wire contract
grpcio-tools>=1.83       # scripts/gen_proto.py
protobuf>=6.30
prometheus-client>=0.21  # engine metrics export
fastapi>=0.115           # single-node SSE server
uvicorn>=0.32
sse-starlette>=2.1
pydantic>=2.9

matplotlib>=3.9      # loss curves
reportlab>=4.0       # phase reports
safetensors>=0.4     # model export
huggingface-hub>=0.24
pytest>=8.0
pytest-timeout>=2.3     # CI-only: a hung test should fail, not burn the job
transformers>=4.44   # test-only: HF Llama parity reference
pypdf>=5.0           # test-only: PDF verification
